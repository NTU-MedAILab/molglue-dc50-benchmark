# GitHub v2 build report

Date: 2026-08-18  
Release scope: public code, metadata, aggregate evidence and figures; private
runtime source rows and predictions excluded.

## Implemented

- merged the exact four-source data builder with the release evidence bundle;
- added the missing hash-matched formal confirmatory protocol;
- preserved every frozen scientific implementation byte-for-byte;
- added a clean-workspace, checkpointed formal CPU orchestrator;
- isolated the 100 conditional permutations into four deterministic CPU
  workers and retained the frozen merge/finalization gate;
- added independent frozen artifact ledgers and a standard-library exact CSV
  verifier;
- added a repository-wide public-boundary check and GitHub Actions workflow;
- replaced row-level repeat tables in the Fig. 1 plotting dependency with one
  redistribution-safe aggregate summary row;
- completed author metadata in `CITATION.cff` and rewrote the Data and Code
  Availability statement for the final no-redistribution route; and
- recorded the graph-model result as GPU reference evidence rather than
  claiming a fresh CPU execution.

## Local gates before formal refit

- builder environment identity: PASS;
- analysis environment identity: PASS;
- source-file hash validation: PASS;
- exact six-table reconstruction: PASS;
- builder unit tests: 8/8 PASS;
- nested builder public-boundary check: PASS;
- repository-wide public-boundary check: PASS; and
- rights-safe Fig. 1--4 render test: PASS.

## Formal clean run

Clean work directory:
`/tmp/molglue_dc50_github_v2_cpu_20260817`

The complete formal CPU chain was launched from an empty directory using exact
local copies of files previously downloaded from the owner endpoints. The
local copies passed the manifest byte-size and SHA-256 gates before dataset
construction. No collaborator-restricted 96-compound material is present or
used. No GPU is requested.

The data build and formal confirmatory core completed. The next matched-context
stage failed closed because its historical implementation requires an exact
parent runtime-manifest byte hash; the regenerated manifest differs because it
contains new runtime metadata. No scientific result mismatch was observed at
that point. The remaining chain has not been run and no process is currently
active.

Final acceptance still requires
`cpu_reproduction_acceptance_v2.json` to report all declared frozen CPU CSV
artifacts as exact and the regenerated figure QA to pass. Until those files
exist, the computational gate remains in progress rather than being reported
as complete.
